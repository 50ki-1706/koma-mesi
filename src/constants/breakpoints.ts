/**
 * Tailwindのブレークポイントに対応するメディアクエリ定数を定義する。
 * JS側の表示判定をTailwindのCSSブレークポイントと一致させるために使う。
 */

/** Tailwindのlgブレークポイント(1024px)に対応するメディアクエリ。 */
export const LG_BREAKPOINT_QUERY = "(min-width: 1024px)";
