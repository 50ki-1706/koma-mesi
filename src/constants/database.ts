/**
 * Database-related constants and configuration.
 * Centralizes default values used across database connection and migration code.
 */

/** Default database URL for local development when DATABASE_URL is not set */
export const DEFAULT_DATABASE_URL = "file:local.db";
