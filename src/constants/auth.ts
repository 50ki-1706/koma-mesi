// Defines navigation constants shared by the authentication UI.
// Environment-specific authentication URLs are resolved in the server configuration.

/** Destination shown after a successful login. */
export const LOGIN_DESTINATION = "/";
